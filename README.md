# Scrapper of kijiji.ca
## Tested only with Python 3.9
Edit `.\db.py` file with required credentials\
Install requirements `pip install -r .\requirements.txt`\
Run `python .\main.py`\
Dump file will be saved to `.\items.sql`